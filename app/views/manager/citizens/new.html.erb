<%= simple_form_for @citizen, url: manager_citizens_path,
                    html: { class: 'relative',
                            data: {
                              controller: 'nested-form',
                              nested_form_wrapper_selector_value: '.nested-form-wrapper'
                            } } do |form| %>
  <div class="flex justify-between items-center mb-4">
    <h1 class="font-bold text-4xl"><%= t('views.manager.citizens.index.add_citizen') %></h1>
    <div>
      <%= form.button :submit, t('views.manager.citizens.save'), class: 'cursor-pointer' %>
    </div>
  </div>
  <%= render "form", citizen: @citizen, form: form %>
<% end %>